<div class="modal-dialogue__backdrop" {{action "close"}}></div>

<div class="modal-dialogue__content">
  <button class="modal-dialogue__close-button" {{action "close"}}>
    {{fa-icon "times"}}
  </button>
  {{yield}}
</div>
